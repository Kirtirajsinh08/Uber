import { createContext } from "react";

export const locationContext = createContext({})
export const hamburgerContext = createContext(false)
export const sourceId = createContext("")
export const destinationId = createContext("")
export const source = createContext('')
export const destination = createContext('')
export const plotOnMap = createContext(false)
export const authenticatedContext = createContext(false)
export const distanceContext = createContext(-1)